// AppProvider placeholder for altarie.js
// Use this to register services into Fastify instance (decorate, hooks, etc.)
export async function register(app) {
  // Example: app.decorate('version', '0.1.0')
}
