{% extends "layouts/base.njk" %}
{% block title %}Blog — Altarie Studio{% endblock %}
{% block content %}
  <h1>Blog</h1>
  <p class="muted">Stories and updates from Altarie Studio.</p>
  <div class="list">
    {% for p in posts %}
      <article class="card item" style="padding:1rem 1.25rem">
        <h3 style="margin:.25rem 0 .25rem"><a href="/blog/{{ p.slug }}">{{ p.title }}</a></h3>
        <p class="muted">{{ p.excerpt }}</p>
        <a class="btn btn-outline" href="/blog/{{ p.slug }}">Read more</a>
      </article>
    {% endfor %}
  </div>
{% endblock %}
