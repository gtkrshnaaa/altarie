<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{% block title %}Altarie — Laravel-like JS{% endblock %}</title>
    <link rel="stylesheet" href="/css/app.css" />
  </head>
  <body>
    <!-- Header with brand badge (Node.js green + pink accent) -->
    <header class="site-header">
      <nav class="site-nav container" style="justify-content:space-between">
        <a class="brand" href="/">
          <span class="brand-badge" aria-hidden="true"></span>
          <span class="brand-name">Altarie <span class="brand-dot">Studio</span></span>
        </a>
        <div class="nav-links" style="display:flex; gap:1rem; align-items:center;">
          <a href="/products" class="nav-link{% if currentUrl and currentUrl.startswith('/products') %} is-active{% endif %}"{% if currentUrl and currentUrl.startswith('/products') %} aria-current="page"{% endif %}>Products</a>
          <a href="/blog" class="nav-link{% if currentUrl and currentUrl.startswith('/blog') %} is-active{% endif %}"{% if currentUrl and currentUrl.startswith('/blog') %} aria-current="page"{% endif %}>Blog</a>
          <a href="/about" class="nav-link{% if currentUrl == '/about' %} is-active{% endif %}"{% if currentUrl == '/about' %} aria-current="page"{% endif %}>About</a>
        </div>
      </nav>
    </header>

    <main class="container">
      <section class="card hero">
        {% block content %}{% endblock %}
      </section>
    </main>

    <footer class="site-footer">
      <div class="container">
        <small>© {{ currentYear or  ("now" | date('YYYY')) }} Altarie Studio · Node.js green with pink accent</small>
      </div>
    </footer>
  </body>
  
</html>
